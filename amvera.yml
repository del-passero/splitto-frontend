version: null
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
  env: 
    VITE_API_BASE: ${VITE_API_BASE}
run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: 80
